#include "main.h"
/**
 * mul - function to multiply two integers
 * @a: first variable
 * @b: second variable
 * Return: Always 0
 */

int mul(int a, int b)
{
	int product;

	product = a * b;

	return (mul);

}
