#include "main.h"
/**
 * _abs - calculates the absolute value of an integer
 * @num: is the integer to be used
 * Return: the absolute value integerr of a number
 */
int _abs(int num)
{
	if (num >= 0)
	{
		return (num);
	}
	return (-num);
}
